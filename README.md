# Zipping and Unzipping with Node.js
## run =>$node zip.js
### For zipping those files which are into forzip directory
## run =>$node unzip.js
### For unzipping those files which are into forunzip directory
